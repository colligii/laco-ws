services:
  api:
    image: minha-imagem-de-chat
    build: .
    command: node dist/index.js # Seu servidor (sem o worker)
    ports:
      - "3001:3001"

  worker:
    image: minha-imagem-de-chat
    build: .
    command: node dist/worker.js # Apenas o seu videoWorker isolado

  worker1:
    image: minha-imagem-de-chat
    build: .
    command: node dist/worker.js # Apenas o seu videoWorker isolado

  worker2:
    image: minha-imagem-de-chat
    build: .
    command: node dist/worker.js # Apenas o seu videoWorker isolado

  worker3:
    image: minha-imagem-de-chat
    build: .
    command: node dist/worker.js # Apenas o seu videoWorker isolado

